# DAO Voting System — Python Dependencies
# Cài đặt: pip install -r requirements.txt

# ── Web3 & Blockchain ─────────────────────────────────────────────────────────
web3==6.15.1                  # Kết nối Ganache, đọc contract
eth-account==0.11.2           # Ký transaction (nếu cần)
eth-typing==4.1.0
eth-utils==4.1.1

# ── Dashboard ─────────────────────────────────────────────────────────────────
streamlit==1.33.0             # Dashboard framework
watchdog==4.0.0               # Hot-reload cho Streamlit

# ── Data & Visualization ──────────────────────────────────────────────────────
pandas==2.2.2                 # Xử lý bảng dữ liệu
plotly==5.21.0                # Biểu đồ tương tác (optional)
altair==5.3.0                 # Biểu đồ Streamlit native

# ── Utilities ────────────────────────────────────────────────────────────────
python-dotenv==1.0.1          # Đọc file .env
requests==2.31.0              # HTTP calls (nếu dùng external API)

# ── Dev / Testing ─────────────────────────────────────────────────────────────
pytest==8.1.1                 # Unit test Python
pytest-asyncio==0.23.6        # Test async (web3 calls)
